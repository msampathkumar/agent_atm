# Demo App 2: ADK App with ATM as Webserver Telemetry

This demonstration application showcases running `agent-atm` with the `remote` data manager. It sends telemetry over HTTP to a standalone central ATM server.

## Quick Setup
```bash
make install
make test
make run
```
